#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <math.h>
#include <ctype.h>
#include <iostream>
#include <queue>

#include "Process.h"

void RR(deque<Process*> processes, int t_cs, int t_slice)
{
    bool simulating = true;
    while(simulating){
        int current_slice = t_slice;
        while(current_slize > 0){

            current_slize--;
        }
    }    
}